# hooks

Hooks React reutilizáveis entre a área do cliente e o admin.

Ainda vazio — nenhum hook compartilhado foi necessário até agora; os
componentes existentes resolvem estado local com os hooks nativos do React.
